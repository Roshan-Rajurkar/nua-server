# Backend Setup (Node.js + Express + MongoDB)

git clone https://github.com/Roshan-Rajurkar/nua-server.git
cd nua-server

<!-- env setup -->

PORT=5000
JWT_SECRET=your_secret
MONGO_URI=your_mongo_url

<!-- start application -->

npm install
